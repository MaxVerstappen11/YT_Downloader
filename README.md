# YT_Downloader

Simple Python-based YouTube downloader to save **videos** and **MP3 audio** locally.

> ⚠️ **Important:** Don’t forget to change the default save file location in the script(s) to your desired folder before using this! (e.g., set custom output paths)  

## 📌 Features

- 🎥 Download YouTube videos
- 🎵 Extract and save audio as MP3
- 📂 Easy to change destination folder where files are saved

## 🛠️ Requirements

Make sure you have the following installed:

- Python 3.8+
- `yt-dlp`  
  Install via pip:
  ```bash
  pip install yt-dlp
